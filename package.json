{
  "name": "next-react",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "fake-db": "json-server -p 4000 --watch mock-db.json",
    "fast-up": "concurrently \"npm run dev\" \"npm run fake-db\""
  },
  "dependencies": {
    "next": "10.0.3",
    "react": "17.0.1",
    "react-dom": "17.0.1",
    "sass": "^1.29.0"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "json-server": "^0.16.3"
  }
}
